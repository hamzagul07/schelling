# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Type checkers / linters
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# uv
# (uv.lock IS committed; the virtualenv is not)

# IDE / OS
.idea/
.vscode/
.DS_Store

# Jupyter
.ipynb_checkpoints

# --- schelling-specific ---
# Raw Jiang transcripts are not redistributable; index is rebuilt locally.
data/transcripts/
# The sqlite-vec knowledge index is a build artifact rebuilt from the transcripts.
data/*.db
# Every solve writes a ForecastRecord here; these are run artifacts, not source.
runs/
# Local analysis working files (situations, sources, drafts) — not part of the engine.
analyses/
# DEU benchmark dataset (CC BY 4.0; downloaded locally, not redistributed here).
data/deu/
